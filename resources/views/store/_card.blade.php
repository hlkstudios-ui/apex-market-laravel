<article class="card"><a class="image" href="{{route('store.show',$product)}}"><img src="{{$product->image_url}}" alt="{{$product->name}}" loading="lazy">@if($product->compare_at_price)<span class="badge">Sale</span>@endif</a><div class="card-body"><p>{{$product->category->name}}</p><a href="{{route('store.show',$product)}}"><h3>{{$product->name}}</h3></a><div><strong>{{$product->formatted_price}}</strong>@if($product->compare_at_price)<s>${{number_format($product->compare_at_price/100,2)}}</s>@endif</div><form method="post" action="{{route('cart.store',$product)}}">@csrf<button aria-label="Add {{$product->name}} to bag">+</button></form></div></article>
