@extends('layouts.app') @section('title','Apex Market — Modern essentials') @section('content')
<section class="hero"><div><p class="eyebrow">The everyday edit</p><h1>Live well.<br><em>Choose better.</em></h1><p>Objects with purpose, selected for the way you live and work.</p><a class="button" href="#collection">Explore the collection</a></div><div class="hero-art"><span>NEW / 2026</span></div></section>
<section class="perks"><span>✦ Considered quality</span><span>↗ Free shipping $100+</span><span>↺ 30-day returns</span></section>
<section class="shop" id="collection"><div class="section-title"><div><p class="eyebrow">Curated essentials</p><h2>Shop the collection</h2></div><p>{{$products->total()}} products</p></div>
<form class="filters"><input name="search" value="{{request('search')}}" placeholder="Search products…"><select name="category"><option value="">All categories</option>@foreach($categories as $category)<option value="{{$category->slug}}" @selected(request('category')===$category->slug)>{{$category->name}} ({{$category->products_count}})</option>@endforeach</select><select name="sort"><option value="">Newest</option><option value="price-low" @selected(request('sort')==='price-low')>Price: low to high</option><option value="price-high" @selected(request('sort')==='price-high')>Price: high to low</option></select><button>Apply</button></form>
<div class="grid">@forelse($products as $product)@include('store._card',['product'=>$product])@empty<div class="empty"><h3>Nothing found</h3><p>Try a different search or category.</p></div>@endforelse</div><div class="pagination">{{$products->links()}}</div></section>
@endsection
