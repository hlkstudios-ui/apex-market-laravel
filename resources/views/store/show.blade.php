@extends('layouts.app') @section('title',$product->name.' — Apex Market') @section('content')<section class="product"><div class="product-image"><img src="{{$product->image_url}}" alt="{{$product->name}}"></div><div class="product-info"><a href="{{route('store.index',['category'=>$product->category->slug])}}">{{$product->category->name}}</a><h1>{{$product->name}}</h1><div class="price">{{$product->formatted_price}} @if($product->compare_at_price)<s>${{number_format($product->compare_at_price/100,2)}}</s>@endif</div><p>{{$product->description}}</p><form method="post" action="{{route('cart.store',$product)}}">@csrf<label>Quantity<input type="number" name="quantity" value="1" min="1" max="{{min(10,$product->stock)}}"></label><button class="button">Add to bag</button></form><ul><li>In stock — {{$product->stock}} available</li><li>Free shipping over $100</li><li>30-day easy returns</li></ul></div></section>@if($related->isNotEmpty())<section class="shop"><div class="section-title"><h2>You may also like</h2></div><div class="grid">@foreach($related as $product)@include('store._card')@endforeach</div></section>@endif @endsection
